# See https://wiki.hypr.land/Configuring/Window-Rules/ for more
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

# No Gaps when there is only one Window in a Workspace
workspace = w[tv1], gapsout:0, gapsin:0
workspace = f[1], gapsout:0, gapsin:0
windowrule = border_size 0, match:float 0, match:workspace w[tv1]
windowrule = rounding 0, match:float 0, match:workspace w[tv1]
windowrule = border_size 0, match:float 0, match:workspace f[1]
windowrule = rounding 0, match:float 0, match:workspace f[1]
# ---

windowrule {
    # Ignore maximize requests from all apps. You'll probably like this.
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    # Fix some dragging issues with XWayland
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}

# Prevent X11 app (like Steam) being scaled by Wayland, which would introduce blur
xwayland {
    force_zero_scaling = true
}

windowrule {
    name = hyprshot-gui
    match:title = Hyprshot
    float = yes
    move = monitor_w-window_w-5 36
}

# Assign applications to Workspaces
# class can be found using: hyprctl clients | grep class

windowrule {
    name = google-chrome
    match:class = ^(google-chrome)$
    workspace = 1
}

windowrule {
    name = visual-studio-code
    match:class = ^(code)$
    workspace = 2
    no_initial_focus = on
}

windowrule {
    name = steam
    match:class = ^(steam)$
    workspace = 3
}

windowrule {
    name = vesktop
    match:class = ^(vesktop)$
    workspace = 4
    no_initial_focus = on
}

windowrule {
    name = slack
    match:class = ^(Slack)$
    workspace = 4
    no_initial_focus = on
}
